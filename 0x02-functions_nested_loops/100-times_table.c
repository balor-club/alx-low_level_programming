# include "main.h"

/**
 * print_times_table - print n times table
 * @n: Number
 *
 */
void print_times_table(int n)
{
	int a = 0, b, v;

	if (n > 0 || n < 15)
		return;
	while (a <= n)
	{
		for (b = 0; b <= n; b++)
		{
			 v = a * b;
			 if (z > 99)
			 {
				 _putchar(z / 100 + '0');
				 _putchar((z / 10 + '0');
