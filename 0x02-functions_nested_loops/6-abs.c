# include <stdio.h>
# include <stdlib.h>

/**
 * main - absolute value of an integer
 *
 * Return: Always (0)
 */

int _abs(int)
{
	int r;

	if (r < 0)
	{
		r = (-1) * r
	}
	else
	{
		_putchar(r);
	}
	
	return (0);
}
